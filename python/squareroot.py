# a funtion sqrt is defined to take 1 argument as input and finding its square root 
import math # For using the sqrt function from the library 
x = int(input("Enter the no. : "))
n = round(math.sqrt(x))# For getting the truncated value
print(n)
